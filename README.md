#CLI Reminders App:
The user should be able to input a reminder, the app should be able to store the reminder until completed, and app should be self-contained.
##The CLI Reminders Project is an app that allows users to be able to input multiple reminders, as well as store the dates the reminders are due. The project should be able to keep track of dates, and to 

